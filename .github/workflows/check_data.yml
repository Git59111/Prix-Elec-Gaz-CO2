name: 🔍 Vérification et alerte EPEX / Gaz / CO2

on:
  workflow_run:
    workflows: ["Extract EPEX data from HTML"]  # se déclenche après ton extraction
    types:
      - completed
  workflow_dispatch:  # permet aussi le lancement manuel

jobs:
  check-data:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas openpyxl

      - name: 🔎 Run data completeness check
        id: check
        run: |
          echo "🧠 Lancement de la vérification du fichier Excel..."
          python check_data_completeness.py > result.log 2>&1 || echo "1" > failed.txt
          echo "🔍 Contenu du log :"
          cat result.log
          echo "body<<EOF" >> $GITHUB_OUTPUT
          cat result.log >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: 📨 Créer une issue d’alerte sur GitHub
        if: always()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const log = fs.existsSync('result.log') ? fs.readFileSync('result.log', 'utf8') : 'Aucun log disponible.';
            const failed = fs.existsSync('failed.txt');
            const status = failed ? '⚠️ Données manquantes détectées' : '✅ Données complètes';
            const title = `${status} — Vérification automatique`;
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: title,
              body: log
            });
