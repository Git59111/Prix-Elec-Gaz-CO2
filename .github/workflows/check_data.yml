name: 🔍 Vérification après extraction EPEX

on:
  workflow_run:
    workflows: ["Extract EPEX data from HTML"]
    types:
      - completed
  workflow_dispatch:  # permet le lancement manuel

jobs:
  check-data:
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas openpyxl

      - name: 🔎 Run data completeness check
        id: check
        run: |
          echo "🧠 Lancement de la vérification du fichier Excel..."
          python check_data_completeness.py > result.log 2>&1 || echo "1" > failed.txt
          echo "🔍 Contenu du log :"
          cat result.log || echo "⚠️ Aucun log généré."

     
      - name: 🧾 Créer une issue GitHub si données manquantes
        if: always()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = './result.log';
            let body = '### 🔍 Rapport de vérification EPEX / Gaz / CO2\n\n';
            if (fs.existsSync(path)) {
              body += fs.readFileSync(path, 'utf8');
            } else {
              body += '⚠️ Aucun fichier `result.log` trouvé.';
            }

            // Vérifie s'il y a des données manquantes
            if (body.includes('Données manquantes') || fs.existsSync('./failed.txt')) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: "[ALERTE] Données manquantes EPEX / Gaz / CO2",
                body: body
              });
              console.log("✅ Issue GitHub créée pour signaler les données manquantes.");
            } else {
              console.log("✅ Aucune donnée manquante détectée — pas d’issue créée.");
            }
